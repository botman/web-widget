# BotMan Web Widget

On Mac via homebrew:

```brew install npm
brew install nvm
npm install -g grunt-cli
npm install -g webpack
npm install -g bower
npm install -g gulp
npm install preact
npm install
npm run-script build```
